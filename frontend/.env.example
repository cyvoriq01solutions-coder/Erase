# Customer authentication API.
# Production should use the same-site Worker custom domain so the secure session cookie
# is not dependent on a third-party workers.dev context.
VITE_API_BASE_URL=https://api.cyvra.co.in
