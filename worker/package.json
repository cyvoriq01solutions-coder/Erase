{
  "name": "@cyvoriq/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "deploy:dry-run": "wrangler deploy --dry-run --env=\"\" --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/pg": "8.15.5",
    "typescript": "5.9.3",
    "wrangler": "4.123.0"
  },
  "dependencies": {
    "pg": "8.16.3"
  }
}
