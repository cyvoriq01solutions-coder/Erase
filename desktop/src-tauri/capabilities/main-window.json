{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum runtime capability for the CYVRA customer shell main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
